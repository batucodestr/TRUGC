import { CreatorRegisterForm } from "@/features/auth/creator-register-form";

export const metadata = { title: "Creator Ol — TRUGC" };

export default function RegisterCreatorPage() {
  return <CreatorRegisterForm />;
}
